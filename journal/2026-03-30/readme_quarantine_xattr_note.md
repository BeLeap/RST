# README quarantine xattr note

- Updated `README.md` running steps with a macOS quarantine workaround command.
- Documented running `xattr -cr /Application/RST.app` before relaunching when macOS blocks the app.
- Renumbered the subsequent run instructions to keep sequence clear.
