# Context menu export all rename

## Summary
- Renamed the combined context-menu action label from `Export WAV + Transcript + Summary` to `Export All` for a cleaner UX.
- Renamed ViewModel method from `exportAllArtifacts()` to `exportAll()` to match UI terminology.
- Updated related export UI strings to stay consistent with the new wording.
